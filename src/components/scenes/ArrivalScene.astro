<section
	class="scene arrival-scene"
	data-arrival-scene
	aria-labelledby="arrival-title"
>
	<div class="arrival-circle" data-arrival-circle aria-hidden="true"></div>

	<header class="arrival-content">
		<h1 id="arrival-title" data-arrival-title>RYAN WYNN</h1>
		<p data-arrival-subtitle>Web Developer</p>
	</header>

	<span class="scroll-indicator" data-scroll-indicator aria-hidden="true"
	></span>
</section>

<style>
	.arrival-scene {
		background: var(--color-surface);
	}

	.arrival-circle {
		position: absolute;
		z-index: 1100;
		inline-size: min(68vw, 42rem);
		aspect-ratio: 1;
		border: 1px solid var(--color-line);
		border-radius: 50%;
		background: var(--color-placeholder);
	}

	.arrival-content {
		position: relative;
		z-index: 1200;
		display: grid;
		place-items: center;
		text-align: center;
	}

	h1 {
		position: relative;
		z-index: 1;
		font-size: clamp(3.5rem, 12vw, 10rem);
		font-weight: 800;
		line-height: 0.84;
		letter-spacing: -0.075em;
	}

	p {
		position: absolute;
		inset-block-start: calc(100% + 1.25rem);
		font-size: clamp(1rem, 2vw, 1.5rem);
		white-space: nowrap;
	}

	.scroll-indicator {
		position: absolute;
		z-index: 1200;
		inset-block-end: var(--scene-gutter);
		inline-size: 1px;
		block-size: clamp(2.5rem, 7vh, 4rem);
		background: var(--color-ink);
	}
</style>
