<section
	class="scene rock-face-scene"
	data-rock-scene
	aria-labelledby="about-statement"
>
	<img
		class="rock-artwork"
		src="/assets/rock/rockface-climbing.webp"
		alt=""
		width="1920"
		height="1080"
		loading="lazy"
		decoding="async"
		draggable="false"
	/>
	<div class="rock-grade" aria-hidden="true"></div>
	<div class="rock-print-frame" aria-hidden="true"></div>

	<p id="about-statement" data-about-statement data-about-panel>
		<span data-about-copy
			>I bring that same energy to every project. Combining creativity,
			thoughtful development, and attention to detail to build digital
			experiences that feel as good as they perform. I’m open to full time
			roles, contract work, and collaborations with people who want to create
			something memorable. If that sounds like your team or project, let’s talk.</span
		>
	</p>
</section>

<style>
	.rock-face-scene {
		isolation: isolate;
		place-items: center end;
		overflow: hidden;
		padding: clamp(1.2rem, 5vw, 5rem);
		background: var(--cobalt);
		color: var(--cream-light);
	}

	.rock-artwork,
	.rock-grade,
	.rock-print-frame {
		position: absolute;
		inset: 0;
		inline-size: 100%;
		block-size: 100%;
	}

	.rock-artwork {
		z-index: -3;
		max-inline-size: none;
		object-fit: cover;
		object-position: center;
		user-select: none;
	}

	.rock-grade {
		z-index: -2;
		background:
			linear-gradient(
				90deg,
				transparent 0 44%,
				rgb(6 28 56 / 8%) 58%,
				rgb(6 28 56 / 50%) 100%
			),
			linear-gradient(180deg, transparent 50%, rgb(6 28 56 / 24%) 100%);
	}

	.rock-print-frame {
		z-index: -1;
		inset: clamp(0.55rem, 1.25vw, 1.25rem);
		inline-size: auto;
		block-size: auto;
		border: clamp(0.22rem, 0.45vw, 0.45rem) solid var(--ink);
		box-shadow: inset 0 0 0 2px rgb(244 228 190 / 38%);
		clip-path: polygon(
			1.2rem 0,
			100% 0,
			100% calc(100% - 1.2rem),
			calc(100% - 1.2rem) 100%,
			0 100%,
			0 1.2rem
		);
	}

	p {
		position: relative;
		z-index: 2;
		max-inline-size: 14ch;
		margin-inline-end: clamp(0rem, 3vw, 3rem);
		padding: clamp(1.1rem, 2.8vw, 2.3rem);
		border: clamp(0.32rem, 0.6vw, 0.6rem) solid var(--ink);
		background: var(--cream-light);
		box-shadow: clamp(0.6rem, 1vw, 1rem) clamp(0.6rem, 1vw, 1rem) 0 var(--red);
		color: var(--ink);
		font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
		font-size: clamp(2.7rem, 5.5vw, 5.9rem);
		font-weight: 900;
		line-height: 0.9;
		letter-spacing: -0.025em;
		transform: rotate(-1.2deg);
	}

	p span {
		display: block;
		font-family: Arial, Helvetica, sans-serif;
		font-size: clamp(1.25rem, min(1.8vw, 2.7vh), 2rem);
		font-weight: 600;
		line-height: 1.45;
		letter-spacing: 0.01em;
		text-align: left;
		text-transform: none;
	}

	@media (max-width: 48rem) {
		.rock-face-scene {
			place-items: end center;
			padding: 1rem;
		}

		.rock-artwork {
			object-position: 38% center;
		}

		.rock-grade {
			background: linear-gradient(
				180deg,
				transparent 34%,
				rgb(6 28 56 / 12%) 50%,
				rgb(6 28 56 / 86%) 100%
			);
		}

		p {
			max-inline-size: min(12ch, calc(100vw - 3rem));
			margin: 0 0 clamp(1.2rem, 7vh, 4rem);
			font-size: clamp(2.6rem, 11.2vw, 4.5rem);
		}

		p span {
			font-size: clamp(1rem, min(4.8vw, 2.8vh), 1.35rem);
		}
	}
</style>
